when:
  - event: push
  - event: pull_request

steps:
  check:
    image: oven/bun:1.3.14-alpine
    commands:
      - apk add --no-cache git
      - bun install --frozen-lockfile
      - bun run check
